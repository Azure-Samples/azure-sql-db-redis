{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "location": {
        "value": "${AZURE_LOCATION}"
      },
      "name": {
        "value": "${SERVICE_WEB_NAME}"
      },
      "imageName": {
        "value": "${SERVICE_WEB_IMAGE_NAME}"
      },
      "applicationInsightsName": {
        "value": "${APPLICATIONINSIGHTS_NAME}"
      },
      "containerAppsEnvironmentName": {
        "value": "${AZURE_CONTAINER_ENVIRONMENT_NAME}"
      },
      "containerRegistryName": {
        "value": "${AZURE_CONTAINER_REGISTRY_NAME}"
      },
      "redisHost": {
        "value": "${REACT_REDIS_HOST}"
      },
      "redisPort":{
        "value": "${REACT_REDIS_PORT}"
      },
      "redisAccessKey": {
        "value" : "${REACT_REDIS_ACCESS_KEY}"
      },
      "readThroughFunctionBaseUrl": {
        "value": "${REACT_READ_THROUGH_FUNCTION_BASE_URL}"
      },
      "keyVaultName": {
        "value": "${AZURE_KEY_VAULT_NAME}"
      },
      "applicationInsightsConnectionString": {
        "value": "${APPLICATIONINSIGHTS_CONNECTION_STRING}"
      },
      "anotherResourceGroup": {
          "value": "${AZURE_ANOTHER_RESOURCE_GROUP}"
      }
    }
  }
  